using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class enemy_HP : MonoBehaviour
{
    static public int HitPoint;
    // Start is called before the first frame update
    void Start()
    {
        HitPoint = 5;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
